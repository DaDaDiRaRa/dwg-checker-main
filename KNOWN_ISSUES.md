# 📋 AutoDWG 알려진 제약사항 / 향후 검토 이슈

본 문서는 **현재 시점에서 수정하지 않기로 결정**했지만, 향후 기능 확장·리팩터링 시
참고할 가치가 있는 제약사항과 사용성 이슈를 기록한 것입니다.
*(2026-05-12 분석 기준 / v6.8)*

---

## 🟡 호환성 / 일반화 관련

### 1. ROI(JSON) 의존성이 너무 큼
**현상**
모든 인식 로직은 사전에 AutoCAD에서 `SET_ROI` LISP로 그려둔 박스 비율 좌표에
의존합니다(`%APPDATA%\AutoDWG_Checker\<블록명>.json`).

**영향**
- 도곽 양식이 다른 회사·프로젝트마다 처음에 다시 SET_ROI를 돌려야 함.
- 박스를 1~2mm만 벗어나게 지정해도 텍스트가 잘려 인식 누락 발생.
- 박스를 너무 크게 잡으면 옆 셀의 텍스트까지 빨려 들어와 도면번호·축척이 오추출.

**향후 검토 방향**
- ROI 자동 추론(블록 ATTDEF 위치 기반)
- Python GUI에서 박스 미리보기·미세조정 기능
- ROI 프리셋 공유(JSON 내보내기/가져오기)

---

### 2. 일반화 어려운 가정들

| 가정 | 깨지는 경우 |
|---|---|
| 도곽이 INSERT(블록) 또는 XREF로 들어가 있다 | 도곽이 LINE/PLINE으로 그려진 도면 → 인식 0건 |
| A1/A3 두 축척만 사용 | A2/A0 도면, "NTS"·"AS NOTED" 표기 → 모두 `X`로 표시 |
| 도면번호 형식: `AA-000-000` | 숫자만(`101-1`), 한글 머리(`설-1`), 영문 4자 이상 prefix(`STRC-001`) → `_도면번호_패턴`의 `{0,4}` 한계 |
| 도면목록표 헤더가 한국어 | 영문 전용 도면(`DRAWING NO`, `TITLE`) → 일부만 인식 |
| 뷰심볼 = 원 + 우측 수평선 | 다이아몬드/사각형 심볼, 좌측으로 뻗는 도면 → 미감지 |

**관련 코드**
- `_도면번호_패턴` — [app.py:98](app.py#L98)
- `GLOBAL_IGNORE_HEADERS` — [app.py:103](app.py#L103)
- `_extract_view_symbols` — [app.py:381](app.py#L381)

---

### 3. 뷰심볼 인식 조건이 까다로움
**현상**
`_extract_view_symbols`에서 뷰심볼로 인정되려면 다음 조건을 모두 만족해야 합니다.
- 원 + 수평 LINE 구조
- **오른쪽 연장 ≥ 원 반지름 × 2**
- **왼쪽 연장 < 오른쪽 연장 × 0.5** (비대칭, 구조 십자선 제외 목적)

**영향**
- 좌측으로 뻗는 뷰심볼, 양쪽 대칭형, 박스/다이아몬드형 심볼은 모두 누락.
- README에는 "원 + 수평선" 정도로만 적혀 있어 사용자가 위 제약을 모름.

**향후 검토 방향**
- 심볼 형태 옵션화(GUI 체크박스로 "좌측형 / 대칭형 / 다이아몬드형" 선택)
- README에 인식 조건 명시

**관련 코드**: [app.py:507-512](app.py#L507-L512)

---

### 4. ODA File Converter 의존
**현상**
- DWG 한 장당 임시 DXF로 변환하는 외부 프로세스 호출.
- 설치 경로는 `C:\Program Files\ODA` 하위만 자동 탐색([app.py:78](app.py#L78)).

**영향**
- 100장 기준 변환 오버헤드 수십 초 ~ 수 분.
- 회사 정책상 다른 경로에 설치하는 환경이면 자동 인식 실패.
- 임시 폴더에 변환 산출물 누적(자동 정리되긴 함).

**향후 검토 방향**
- 사용자 지정 ODA 경로 설정 GUI
- 변환 결과 캐싱(같은 DWG 재분석 시 변환 생략)
- LibreDWG·oda 등 대체 엔진 비교 검토

---

### 5. 동일 도면번호 중복 처리
**현상**
- 목록표에 같은 도면번호가 두 번 들어 있으면 `drop_duplicates(subset=["도면번호(LIST)"])`로
  두 번째 행 이후는 버려집니다.
- v6.8 패치 시점에 **중복 발견 시 경고 로그**는 추가되었으나, 두 행 모두 보존하는
  처리는 아직 안 됨([app.py:875](app.py#L875)).

**영향**
- 목록표 작성자가 의도적으로 같은 번호의 변경본(rev.A, rev.B)을 두 줄로 적은 경우 손실.

**향후 검토 방향**
- 중복 행을 별도 시트로 분리해 출력
- KEY를 `도면번호 + 도면명 해시`로 변경하는 옵션

---

## 🟠 UX / 사용성 관련

### 6. ROI 미리보기 부재
**현상**
SET_ROI를 AutoCAD에서 돌린 뒤, 박스가 어디에 잡혔는지 확인하려면
다시 캐드로 돌아가 도면을 열어봐야 합니다. Python GUI 측에서는 미리보기 없음.

**영향**
- 시행착오 시 캐드 ↔ EXE 왕복이 늘어나 첫 진입 비용↑.
- 박스가 살짝 어긋난 것을 사용자가 즉시 알기 어려움.

**향후 검토 방향**
- Python GUI에 도곽 썸네일 + ROI 사각형 오버레이 표시
- "테스트 추출" 버튼으로 도면 1장만 즉시 추출해 결과 확인

---

### 7. EXE 배포 시 LISP 파일 분리
**현상**
[build.spec](build.spec)는 `SET_ROI.lsp`를 EXE에 묶지 않습니다.

**영향**
- EXE만 받은 사용자는 LISP 파일이 없어 첫 실행 시 "JSON 파일 없음" 오류 발생.
- 배포 시 별도로 LSP를 함께 보내야 하는 번거로움.

**향후 검토 방향**
- spec 파일의 `datas`에 `SET_ROI.lsp` 포함
- EXE 첫 실행 시 `%USERPROFILE%\Documents\AutoDWG\` 등에 자동으로 풀어주기
- 또는 GUI 안에 "LISP 파일 내보내기" 버튼

---

### 8. "동/그룹" 휴리스틱이 강함
**현상**
`_extract_group_from_title`([app.py:124-141](app.py#L124-L141))은 도면명 앞 첫 토큰이
**순수 한글**이면 그룹으로 판정합니다.

**영향**
- `토목 도면`, `기계 도면` 같은 단순 분류 단어가 그룹으로 잘못 잡힐 수 있음.
- 그 결과 도면명·그룹 비교에서 false-positive 불일치(빨간색)가 발생할 가능성.

**향후 검토 방향**
- 그룹으로 인정할 단어 화이트리스트 또는 블랙리스트 옵션화
- GUI에 "그룹 분리 사용 여부" 체크박스
- 일정 길이 이상 한글 토큰(예: 4글자↑)은 그룹 후보에서 제외

---

## 📌 참고

- 본 문서에 적힌 항목은 모두 **현재 동작에 치명적이지 않은 제약**이며,
  사용 환경(사내 표준 도곽 + 한국어 AA-xxx-xxx 번호 체계)에서는 큰 문제가 없습니다.
- 향후 다른 회사·다른 양식의 도면으로 적용 범위를 넓힐 때 우선순위 검토 대상입니다.
- 명백한 버그/오류(`requirements.txt` 누락, 멀티스레드 안전성, 도곽 매칭 부분일치 등)는
  v6.8 패치 시점에 모두 처리되었습니다.
