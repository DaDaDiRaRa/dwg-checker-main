# AutoDWG 알려진 제약사항 / 향후 검토 이슈

갱신: 2026-06-10 / v6.8

---

## 이번 세션에서 수정된 항목 (v6.8)

| # | 항목 | 수정 내용 |
| --- | --- | --- |
| 1 | UPX 압축으로 AV 오탐 | `build.spec` → `upx=False` |
| 2 | `%TEMP%` 한글 경로 EXE 크래시 | `runtime_tmpdir="C:\\ProgramData\\AutoDWGChecker"` |
| 3 | ODA 변환 시 한글 경로 에러 | `_cad_로드()` — 비ASCII 경로를 ASCII 임시 경로로 복사 후 변환 |
| 4 | `ProcessPoolExecutor` 워커가 로거·GUI 초기화 | 메인 프로세스 여부 확인 후 `_setup_file_logger()` 호출 |
| 5 | 동일 타임스탬프에 덮어쓰기 충돌 | 리포트 파일명 → `도면검토리포트_YYYYMMDD_HHMMSS.xlsx` 타임스탬프 적용 |
| 6 | PermissionError 메시지가 한 가지뿐 | 파일 존재 여부에 따라 "파일 열려 있음" / "쓰기 권한 없음" 두 경로 분기 |
| 7 | LISP 파일 EXE 미포함 (KNOWN #7) | `build.spec` `datas`에 `SET_ROI.lsp` 추가 + 첫 실행 시 EXE 폴더에 자동 추출 |
| 8 | 멀티프로세스 워커에서 `freeze_support()` 누락 | `__main__`에 `multiprocessing.freeze_support()` 추가 |
| 9 | 워커 수 하드코딩 | `max_workers=min(4, os.cpu_count() or 4)` |
| 10 | DWG 측 그룹/동 추출로 false-positive 69건 (KNOWN #8) | `_process_single_dwg()` — DWG 측 그룹/동 추출 제거, 원문 그대로 비교 |
| 11 | 리포트 열 구성에 `구분_DWG(그룹)` 불필요 | `build_report()` — 해당 열 및 그룹 불일치 로직 제거 |
| 12 | `최종_저장경로` 미초기화로 PermissionError 처리 시 NameError | `최종_저장경로 = None` 초기화 추가 |
| 13 | `readme.html` 내용이 v6.7 기준 | v6.8 신기능 전부 반영하여 전면 재작성 |

---

## 코드 수정 보류 중인 항목

### A. 첫 실행 지연 + 복수 인스턴스 방지 없음

**현상:** `console=False` EXE는 첫 실행 시 PyInstaller 압축 해제 + ODA 초기화로 5~15초 지연. 사용자가 "안 켜지는 줄 알고" 더블클릭을 반복하면 인스턴스가 중복 실행됨.

**영향:** 멀티프로세싱 + ODA EXE가 동시에 여러 벌 뜰 경우 PC 부하 폭증, 간헐적 크래시.

**향후 검토 방향:**

- `mutex` 또는 named pipe로 단일 인스턴스 보장
- 스플래시 이미지(tkinter Toplevel)로 첫 실행 진행 상황 표시

---

### B. `console=False` 크래시 시 사용자 피드백 없음

**현상:** GUI가 표시되기 전 초기화 중 예외 발생 시 로그·알림 없이 프로세스가 그냥 종료됨.

**영향:** 사용자는 "프로그램이 안 켜진다"는 것만 알고, 원인을 알 수 없음.

**향후 검토 방향:** `__main__` 전체를 `try/except`로 감싸고, 예외 발생 시 로그 경로를 담은 `messagebox.showerror` 팝업 표시.

```python
if __name__ == "__main__":
    try:
        multiprocessing.freeze_support()
        ...
        app.mainloop()
    except Exception:
        import traceback
        log_path = os.path.join(os.environ.get("APPDATA",""), "AutoDWG_Checker", "autodwg.log")
        msg = f"초기화 중 오류가 발생했습니다.\n로그: {log_path}\n\n{traceback.format_exc()}"
        try:
            import tkinter.messagebox as mb
            mb.showerror("AutoDWG 오류", msg)
        except Exception:
            pass
```

---

## 구조적 한계 (설계 결정 수준)

### 1. ROI(JSON) 의존성

**현상:** 모든 인식 로직은 AutoCAD에서 `SET_ROI` LISP로 그려둔 박스 비율 좌표에 의존.

**영향:**

- 도곽 양식이 바뀌면 SET_ROI 재실행 필요.
- 박스를 1~2mm 벗어나게 지정해도 텍스트 잘림/누락 발생.
- 박스가 너무 크면 인접 셀 텍스트 오추출.

**향후 검토 방향:**

- ROI 자동 추론 (블록 ATTDEF 위치 기반)
- Python GUI에서 박스 미리보기·미세조정
- ROI 프리셋 공유 (JSON 내보내기/가져오기)

---

### 2. 일반화 어려운 CAD 구조 가정

| 가정 | 깨지는 경우 |
| --- | --- |
| 도곽이 INSERT(블록) 또는 XREF | 도곽이 LINE/PLINE으로 그려진 도면 → 인식 0건 |
| A1/A3 두 축척만 사용 | A2/A0 도면, NTS·AS NOTED 표기 → `X`로 표시 |
| 도면번호 형식 `AA-000-000` | 숫자만(`101-1`), 한글 머리(`설-1`), 영문 4자↑(`STRC-001`) → `_도면번호_패턴` `{0,4}` 한계 |
| 도면목록표 헤더가 한국어 | 영문 전용 도면(`DRAWING NO`, `TITLE`) → 일부만 인식 |
| 뷰심볼 = 원 + 우측 수평선 | 다이아몬드/사각형 심볼, 좌측으로 뻗는 도면 → 미감지 |

---

### 3. 뷰심볼 인식 조건이 까다로움

인식 조건 (모두 만족해야 함):

- 원 + 수평 LINE 구조
- 오른쪽 연장 ≥ 원 반지름 × 2
- 왼쪽 연장 < 오른쪽 연장 × 0.5 (구조 십자선 제외)

**영향:** 좌측형, 양쪽 대칭형, 박스/다이아몬드형 심볼은 모두 누락.

**향후 검토 방향:**

- GUI 체크박스로 "좌측형 / 대칭형 / 다이아몬드형" 선택 옵션화
- README에 인식 조건 명시

---

### 4. ODA File Converter 외부 의존

**영향:**

- 도면 1장당 DXF 변환 오버헤드 (100장 기준 수십 초~수 분).
- 기본 경로(`C:\Program Files\ODA`) 외 설치 시 자동 인식 실패 (→ 항목 C 참조).
- AutoCAD LT 환경에서 별도 LISP 실행 불가.

---

### 5. 동일 도면번호 중복 처리 없음

**현상:** 목록표에 같은 도면번호가 두 번 이상 있으면 `drop_duplicates`로 두 번째 행 이후 소실. 중복 발견 시 경고 로그는 출력됨.

**향후 검토 방향:**

- 중복 행을 별도 시트로 분리 출력
- KEY를 `도면번호 + 도면명 해시`로 변경하는 옵션

---

### 6. ROI 미리보기 부재

**영향:** 박스가 어긋난 것을 알려면 캐드 ↔ EXE를 왕복해야 함. 첫 진입 비용 높음.

**향후 검토 방향:**

- Python GUI에 도곽 썸네일 + ROI 사각형 오버레이 표시
- "테스트 추출" 버튼으로 도면 1장만 즉시 추출해 결과 확인

---

### 7. ~~EXE 배포 시 LISP 파일 분리~~ → v6.8에서 해결

`build.spec`의 `datas`에 `SET_ROI.lsp` 포함 + 첫 실행 시 EXE 폴더에 자동 추출.

---

### 8. LIST 측 그룹 휴리스틱 (2-pass 시스템)

**현상:** `extract_dwg_list_table()`의 2-pass 그룹 추출은 도면목록표 전체 컨텍스트 기반 — 동일 prefix 연속 등장 시 도면명 일부로, 1회 등장 시 그룹 헤더로 처리 후 아래 행에 전파.

**해결된 문제:** DWG 측 그룹 추출 제거(v6.8)로 false-positive 69건 해소. LIST 측 그룹 칼럼은 참고용으로 유지 (비교 대상에서 제외됨).

**남은 한계:**

- `토목`, `기계`, `전기` 등 분류 키워드가 도면명 첫 토큰인 경우 그룹으로 오판될 수 있음
- 3글자 이하 한글 토큰은 그룹으로 판정됨 — 순수 한글 첫 토큰 = 그룹이라는 가정이 너무 강함

---

## 실제 데이터 분석에서 새로 발견된 문제

> 2026-06-08 `도면검토리포트_20260608_170442.xlsx` 분석 결과

### N1. DWG 도면명에 분류 prefix가 없는 경우

**현상:** 도면목록표에는 `형별` / `발코니난간` / `비구조요소`가 도면명 앞에 붙어 있으나, 실제 CAD 도면 제목란에는 해당 prefix가 없음.

예: LIST `형별 A1형 평면도` → DWG `A1형 평면도` → 불일치(빨간색)

**근본 원인:** CAD 도면 자체의 제목란이 목록표와 다르게 작성된 경우. 소프트웨어 수정으로 해결 불가.

**권고:** 원본 도면의 제목란 수정 또는 목록표 정정.

---

### N2. 구분자 표기 차이

**현상:** 목록표와 CAD 도면에서 구분자 형식이 다름.

- LIST `관리사무소/경로당` → DWG `관리사무소 / 경로당` (공백 포함)
- LIST `입,단면도` → DWG `입면도/단면도` (쉼표 축약형 vs 전체 표기)

**현재 대응:** `_expand_title_keywords()`가 쉼표 축약형 일부를 전개하지만, 공백 포함 슬래시는 미처리.

**향후 검토 방향:** 비교 전 공백·구분자 정규화 함수 추가 (슬래시 앞뒤 공백 제거 등).

---

### N3. 도면명이 중복 prefix로 시작되는 경우

**현상:** CAD 도면 제목란에 동일 단어가 두 번 입력됨.

예: LIST `주출입구 평면도` → DWG `주출입구 주출입구 평면도` (중복 입력)

**근본 원인:** 도면 작성자의 CAD 입력 오류.

**권고:** 원본 도면 수정.

---

## 참고

- 치명적 버그 (requirements.txt 누락, 멀티스레드 안전성, 한글 경로 크래시 등)는 v6.8에서 모두 처리됨.
- N1~N3는 소프트웨어 수정으로 해결 불가한 CAD 원본 데이터 품질 문제.
- A~C는 소프트웨어 수정으로 해결 가능하나 우선순위 낮은 항목.
- 1~8은 현재 사용 환경(사내 표준 도곽 + 한국어 AA-xxx-xxx 번호 체계)에서는 치명적이지 않음.
